const config = {
  host: "127.0.0.1", // IP for localhost
  port: 3306,
  user: "root", // Your MySQL username
  password: "admin", // Your MySQL password
  database: "employee_leave_management", // Your database name
};

module.exports = config;
